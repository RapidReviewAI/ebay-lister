export interface MasterItem {
  id: string;
  title: string;
  description: string;
  price: string;
  category: string;
  categoryId: string;
  condition: string;
  photos: string[];
  brand?: string;
  size?: string;
  color?: string;
  department?: string;
}
